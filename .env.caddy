# =============================================================================
# CADDY CORE SETTINGS
# =============================================================================
# Optional proxy tuning values were moved to .env.optional.

# Use the internal ACME CA so local domains issue instantly without public DNS.
CADDY_ACME_CA=internal
CADDY_AUTO_HTTPS=internal

# Ensure the docker-proxy plugin watches the same network as application services.
CADDY_DOCKER_NETWORK=lightrag_frontend
