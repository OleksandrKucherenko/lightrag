# =============================================================================
# CADDY REVERSE PROXY CONFIGURATION
# =============================================================================

#
# Contact email used when Caddy obtains development certificates.
#
CADDY_EMAIL=admin@dev.localhost

#
# ACME certificate authority endpoint leveraged for local TLS.
#
CADDY_ACME_CA=internal  # Use Caddy's internal CA for local development

#
# Disables upstream certificate verification for proxied services.
#
CADDY_SKIP_VERIFY=true

#
# Interval between Docker API polls for container changes.
#
CADDY_DOCKER_POLLING_INTERVAL=5s

#
# Interval for refreshing dynamic configuration from discovered containers.
#
CADDY_DOCKER_REFRESH_INTERVAL=30s

#
# Docker network name Caddy attaches to for service discovery.
#
CADDY_DOCKER_NETWORK=lightrag_frontend

#
# Controls how Caddy provisions HTTPS for discovered hosts.
#
CADDY_AUTO_HTTPS=internal

#
# Address where the Caddy admin API listens.
#
CADDY_ADMIN_LISTEN=:2019

#
# Log level used by the reverse proxy.
#
CADDY_LOG_LEVEL=INFO

#
# Location of the access log emitted by Caddy.
#
CADDY_ACCESS_LOG=/var/log/caddy/access.log

#
# Location of the error log emitted by Caddy.
#
CADDY_ERROR_LOG=/var/log/caddy/error.log
