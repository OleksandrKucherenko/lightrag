# =============================================================================
# LIGHTRAG SERVICE CONFIGURATION
# =============================================================================

#
# Address the LightRAG API binds to inside the container.
#
HOST=0.0.0.0

#
# Port the LightRAG API listens on inside the container.
#
PORT=9621

#
# Title displayed in UIs that consume the LightRAG API.
#
WEBUI_TITLE=LightRAG Production

#
# Short description displayed in UIs that consume the LightRAG API.
#
WEBUI_DESCRIPTION=Production RAG System with Knowledge Graphs

#
# Comma-separated list of origins allowed by CORS for the API.
#
CORS_ORIGINS=http://localhost:3000,https://dev.localhost

#
# Number of worker processes/threads the API will spawn.
#
WORKERS=8

#
# Directory where incoming documents are placed for ingestion.
#
INPUT_DIR=/app/data/inputs

#
# Directory where LightRAG stores embeddings, indexes, and metadata.
#
WORKING_DIR=/app/data/rag_storage

#
# Directory for application logs inside the container.
#
LOG_DIR=/app/logs

#
# Implementation used for key-value storage (e.g., RedisKVStorage).
#
LIGHTRAG_KV_STORAGE=RedisKVStorage

#
# Implementation used to track document processing status.
#
LIGHTRAG_DOC_STATUS_STORAGE=RedisDocStatusStorage

#
# Implementation used for vector storage (e.g., QdrantVectorDBStorage).
#
LIGHTRAG_VECTOR_STORAGE=QdrantVectorDBStorage

#
# Implementation used for graph storage (e.g., MemgraphStorage).
#
LIGHTRAG_GRAPH_STORAGE=MemgraphStorage

#
# Hostname of the Redis service used by LightRAG.
#
REDIS_HOST=kv

#
# Port of the Redis service used by LightRAG.
#
REDIS_PORT=6379

#
# Connection URI for Redis used by LightRAG (overrides host/port).
#
REDIS_URI=redis://kv:6379

#
# Logical workspace/namespace used when writing Redis keys.
#
REDIS_WORKSPACE=production

#
# HTTP endpoint of the Qdrant vector database used by LightRAG.
#
QDRANT_URL=http://vectors:6333

#
# Logical workspace/namespace used by LightRAG for Qdrant collections.
#
QDRANT_WORKSPACE=production

#
# Bolt connection URI for Memgraph used by LightRAG.
#
MEMGRAPH_URI=bolt://graph:7687

#
# Logical workspace/namespace used by LightRAG for Memgraph graphs.
#
MEMGRAPH_WORKSPACE=production

#
# Global workspace name for isolating all LightRAG resources.
#
WORKSPACE=production

# ref: https://github.com/HKUDS/LightRAG/blob/main/lightrag/api/README.md

#
# Provider binding LightRAG uses for LLM inference (e.g., openai).
#
LLM_BINDING=openai

#
# Default model identifier used for text generation/completions.
#
LLM_MODEL=gpt-4o-mini

#
# Base URL for the LLM provider API used by LightRAG.
#
LLM_BINDING_HOST=https://api.openai.com/v1

#
# Embedding provider binding LightRAG uses (e.g., openai).
#
EMBEDDING_BINDING=openai

#
# Base URL for the embedding provider API.
#
EMBEDDING_BINDING_HOST=https://api.openai.com/v1

#
# Default embedding model identifier used by LightRAG.
#
EMBEDDING_MODEL=text-embedding-3-small

#
# Expected embedding vector dimensionality for the configured model.
#
EMBEDDING_DIM=1536
