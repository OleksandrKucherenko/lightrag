# Normalize everything to LF in Git
* text=auto eol=lf

# Scripts must always use LF
*.sh text eol=lf

# Windows-specific stuff (ok to be CRLF)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf