---
# DISABLED: This monitoring service (Isaiah) is designed for Docker environments
# and requires Docker socket access which is not available/recommended in K8s.
#
# For Kubernetes monitoring, use native tools instead:
# - Kubernetes Dashboard: kubectl proxy (access via http://localhost:8001/api/v1/namespaces/kubernetes-dashboard/services/https:kubernetes-dashboard:/proxy/)
# - Lens: https://k8slens.dev/
# - k9s: https://k9scli.io/
# - Prometheus + Grafana
# - kubectl top pods/nodes
#
# To enable this (NOT RECOMMENDED):
# 1. Uncomment the entire manifest below
# 2. Ensure hostPath docker socket access is configured
# 3. Update deploy.sh to deploy this file
#
# ---
# MANIFEST DISABLED - Uncomment below to enable (not recommended)
# ---

# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: monitor
#   namespace: lightrag
#   labels:
#     app.kubernetes.io/name: monitor
#     app.kubernetes.io/component: monitoring
#     app.kubernetes.io/part-of: lightrag-stack
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app.kubernetes.io/name: monitor
#   template:
#     metadata:
#       labels:
#         app.kubernetes.io/name: monitor
#         app.kubernetes.io/component: monitoring
#     spec:
#       containers:
#       - name: monitor
#         image: ghcr.io/will-moss/isaiah:1.36.2
#         ports:
#         - name: http
#           containerPort: 3000
#           protocol: TCP
#         volumeMounts:
#         - name: docker-sock
#           mountPath: /var/run/docker.sock
#           readOnly: true
#         resources:
#           limits:
#             memory: 1Gi
#             cpu: 500m
#           requests:
#             memory: 512Mi
#             cpu: 250m
#         livenessProbe:
#           httpGet:
#             path: /
#             port: 3000
#           initialDelaySeconds: 30
#           periodSeconds: 10
#           timeoutSeconds: 5
#         readinessProbe:
#           httpGet:
#             path: /
#             port: 3000
#           initialDelaySeconds: 5
#           periodSeconds: 10
#           timeoutSeconds: 1
#       volumes:
#       - name: docker-sock
#         hostPath:
#           path: /var/run/docker.sock
#           type: Socket
# 
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: monitor
#   namespace: lightrag
#   labels:
#     app.kubernetes.io/name: monitor
#     app.kubernetes.io/component: monitoring
# spec:
#   type: ClusterIP
#   ports:
#   - port: 3000
#     targetPort: 3000
#     protocol: TCP
#     name: http
#   selector:
#     app.kubernetes.io/name: monitor
