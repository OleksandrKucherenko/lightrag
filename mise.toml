[settings]
experimental = true

[tasks.setup]
description = "Preconfigure project folders"
run = [
    "mkdir -p docker/data/caddy",
    "mkdir -p docker/etc/caddy"
]

[hooks]
enter = ["mise run setup"]

[tools]
mkcert = "latest"
